# Esta carpeta contiene scripts de smoke test para la API.
# Puedes ejecutar estos scripts tras despliegues para validar conectividad y existencia de endpoints.

#Recuerda instalar las dependencias (requests, pyyaml) en tu entorno si no están presentes para ejecutar el smoke test.
